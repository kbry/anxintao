<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="android.com.anxintao">

  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />

  <application
    android:name=".AnxintaoApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".goodDetail.GoodDetailActivity" />
    <activity android:name=".store.DataActivity" />
    <activity android:name=".store.SSSYActivity" />
    <activity android:name=".store.MyCustomerActivity" />
    <activity android:name=".store.CustomerOrderActivity" />
    <activity android:name=".user.UserRightsIntroActivity" />
    <activity android:name=".user.MyWalletActivity" />
    <activity android:name=".user.LeftMoneyActivity"/>
    <activity android:name=".user.GetMoneyActivity"/>
  </application>

</manifest>